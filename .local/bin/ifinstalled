#!/bin/sh
# check if a program is installed
for i  in "$@"; do
	if ! which "$i" >/dev/null 2>&1 && ! pacman -Qq "$i" >/dev/null 2>&1; then
		echo "$i is not installed. Please install it."
		exit 1
	fi
done
