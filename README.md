# rumi's dotfiles

(work-in-progress) my configuration files for Artix/Arch Linux. To be deployed by a script I am working on.

- The home directory is very minimal and most of the config is in `~/.config`
- To be used with sxwm and sxbar (see [uint23](https://github.com/uint23))
